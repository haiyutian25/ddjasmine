# 插件化框架增强与优化清单

> 记录时间：2026-08-24
> 范围：`android/core-plugin`（Kotlin 运行时）+ `rust/plugin-core`（决策核心）+ `core-plugin-ksp`（权限织入）
> 结论：框架骨架完整扎实，剩余问题集中在「安全声明式 vs 强制式」「功能完整性硬缺口」「健壮性与工程化欠账」三类。

---

## 总体结论

框架已经具备：事务化安装、签名/摘要裁决、崩溃分类、进程隔离、跨进程桥、能力声明、dlopen 桥、资产下载、native 生命周期钩子、跨插件服务（`ServiceKey`）。

剩余问题分三类：

1. **安全是"声明式"而非"强制式"** —— 能力/权限只裁决、不拦截。
2. **功能完整性的硬缺口** —— Activity 多占坑、Service 跨进程 bind、插件依赖声明。
3. **健壮性与工程化欠账** —— 资源释放、缓存配额、并发一致性、零单测。

---

## P0 — 安全（必须补，否则能力/权限机制是"纸面"的）

### 1. 能力声明缺少运行时强制门控

**现状**：`Capability`（Exec/Gpu/Network/Storage/Camera）在**安装时**经
`adjudicate_capabilities` 裁决，但**运行时**只有 `ExecBridge` 调用了
`checkCapability(EXEC)`。GPU/NETWORK/STORAGE/CAMERA 四个能力**没有任何运行时拦截点**
——插件声明"我需要 network"获得授权后，实际访问网络时框架完全不拦。

**问题**：能力机制目前是"安装时问你一次，之后随便用"，对 MNN（GPU/网络）、
Proot（存储/网络）这类敏感能力，等于没有强制。

**方案**：
- 提供框架级门控入口，插件访问敏感能力**必须**经此：
  - `PluginHost.requireCapability(FfiCapability.NETWORK, pluginId)` — 网络前调用
  - `PluginHost.requireCapability(FfiCapability.STORAGE, pluginId)` — 大文件 IO 前调用
- 把 `checkCapability` 的 `hardFail` 语义做实：未声明/未授权 → 抛 `SecurityException`，而非静默返回。
- 在 `PluginApis` 补一套"门控包装"，让"忘记门控"在编译期就不可能（复用 KSP `@GatedApi` 思路）。

### 2. 插件 `uses-permission` 未映射到宿主

**现状**：插件 manifest 声明的权限（`RECEIVE_BOOT_COMPLETED`、`POST_NOTIFICATIONS` 等）
**不会合并进宿主**，插件以宿主权限运行。插件声明了权限，实际取决于宿主是否"碰巧"声明了。

**问题**：插件开发者以为自己声明了权限就能用，这是插件化框架的经典陷阱，目前没有任何机制暴露/校验。

**方案**：
- 安装时用 `parseReceivers` 同款 `AssetManager` 路径**解析插件的 `uses-permission` 清单**，存入 `PluginRecord`（或独立 JSON）。
- 提供 `PluginHost.requiredPermissionsOf(pluginId): List<String>` 查询，宿主据此在 manifest 预声明。
- 权限校验：插件声明了宿主没有的权限时，在安装/加载时告警（`loadFailureCallback` 或日志）。

---

## P1 — 功能完整性硬缺口

### 3. Activity 单占坑：不支持 launchMode / 主题 / 方向

**现状**：所有插件 Activity 共用一个 `HostActivity`，`startPluginActivity` 只传类名。
插件 Activity 无法声明 `launchMode`、`screenOrientation`、`theme`、`windowSoftInputMode`。

**问题**：登录页 singleTask、横屏视频页、透明主题对话框页等真实场景是刚需，单占坑会互相冲突。

**方案**：
- manifest 预注册**多个占坑** Activity（`HostActivityStandard` / `HostActivitySingleTop` /
  `HostActivitySingleTask` / `HostActivityTransparent`），各带不同 `launchMode`/`theme`。
- `startPluginActivity` 增加 `launchMode` 参数，选择对应占坑。
- 或插件 `PluginEntry` 声明 `activities` 表（类名 → 占坑类型），框架按表路由。

### 4. Service 跨进程 bind（onBind 代理）

**现状**：`bindPluginService` 返回宿主 `HostService` 的连接，同进程 OK。但**跨进程绑定时**
（宿主 bind 隔离进程里的插件服务），插件 IBinder 无法直接跨越。

**问题**：进程隔离做完后，插件 Service 的 bind 链路在隔离进程下是断的。

**方案**：
- 隔离进程里的插件 `onBind` 返回值，经 `RemoteServices` 的 Binder 桥注册。
- 宿主侧 `bindPluginService` 走桥拿到 token，包装成 `IBinder` 返回给绑定方。

### 5. 插件间依赖缺少声明机制

**现状**：Rust 侧有完整的 `dependenciesChain` / `dependentsChain` / 借边 / `restartPlan`，
但**插件如何声明"我依赖另一个插件"**没有任何入口——没有 meta-data 约定。

**问题**：拓扑依赖核心逻辑目前**没有业务入口**，等于闲置。

**方案**：
- 新增 meta-data `jasmine.plugin.dependencies = "b.plugin.id,c.plugin.id"`。
- 安装时解析，随 `FfiPluginRecord` 传入 Rust，落进 `topology` 的边。
- 加载时按依赖序加载、卸载时 `dependentsChain` 先卸载依赖方。

---

## P2 — 健壮性

### 6. 资源未缓存、卸载不释放

**现状**：`PluginResourcesLoader.load` 每次 load 都 `new AssetManager` + `new Resources`；
`LifecycleExecutor.unload` 移除 `LoadedPlugin` 后，`AssetManager` 没有显式 `close()`
（API 30+ 才有），靠 GC 回收 native 资源，不及时。

**方案**：
- `LoadedPlugin` 增加 `release()`，卸载时 API 30+ 调 `assets.close()`。
- 资源按 `(apkPath, config)` 做进程内缓存，重复 load 复用（配置变化时失效）。

### 7. 资产缓存无配额

**现状**：`PluginAssetsProvider` 的 `assets-cache` 目录**无限增长**，没有清理。

**方案**：给 `assets-cache` 加 LRU/总大小上限，超限清理最旧文件（复用 `AssetDownloader.enforceQuota` 逻辑）。

### 8. ledger 跨进程并发一致性

**现状**：隔离进程与宿主**共享同一个 `plugins.json`**。`commitInstall` 用 `atomic_write`
（rename 语义），但两个进程**各自持有独立内存态**——宿主 install 写盘后，隔离进程的内存
ledger 不知道新插件，反之亦然。

**问题**：宿主装/卸插件后，隔离进程的 `locateClass`/`routeAuthority` 用的是过期内存态。

**方案**：
- 最小方案：隔离进程在关键操作前 `repair()` 重新读盘，或 `FileObserver`/广播在 ledger 变更时触发重载。
- 完整方案：引入跨进程文件锁（`flock`），或把 ledger 改为宿主独占写 + 隔离进程只读快照。

---

## P3 — 工程化

### 9. Kotlin 侧零单元测试

**现状**：Rust 侧 148 个测试全绿，但 **core-plugin 的 Kotlin 侧 0 个测试**。
`InstallExecutor`（元数据解析/ABI 提取/exec 提取）、`LifecycleExecutor`、
`PluginClassLoader` 委托链、`PluginProcessBridge` Binder 语义——这些纯逻辑单元都无覆盖。

**方案**：为纯逻辑模块补 JVM 单测。

### 10. 可观测性缺失

**现状**：框架内部无统一日志/埋点。加载失败、能力拒绝、崩溃分类、进程隔离切换等关键
事件没有统一可观测出口。

**方案**：
- 新增 `PluginEventListener`（load/unload/install/crash/isolate 事件流），宿主可选订阅。
- 关键路径打结构化日志（tag 统一 `PluginRuntime`）。

---

## 建议落地顺序

```
P0-1 能力运行时门控   ──┐
P0-2 权限映射/校验      ├─ 安全底座，先做
P1-5 插件依赖声明      ──┘ （依赖声明同时解锁 Rust 拓扑的存量逻辑）
P1-3 Activity 多占坑
P1-4 Service 跨进程 bind   （依赖第2处跨进程桥，条件已具备）
P2-6/7 资源缓存+配额
P2-8 ledger 并发一致
P3-9/10 测试+可观测
```

---

## 实施状态（2026-08-24）

八项全部落地。Rust 148 测试通过、Kotlin Robolectric 3 单测通过、`assembleRelease` 构建成功。

### P0-1 能力运行时强制门控 ✅
- `PluginHost.hasCapability` / `requireCapability`：能力门控改为"安装时声明"校验，
  未声明即抛 `SecurityException`（硬门控）。
- `ExecBridge.runNative` / `run` 改用 `requireCapability(EXEC, pluginId)`。

### P0-2 插件 uses-permission 映射/校验 ✅
- `InstallExecutor.parsePermissions`（PackageManager GET_PERMISSIONS）+ `writePermissions`/`readPermissions` 持久化。
- `PluginHost.requiredPermissionsOf(pluginId)` 查询；安装时 `warnMissingHostPermissions` 告警宿主未声明权限。

### P1-5 插件间依赖声明 ✅
- Rust `PluginCore.declare_dependencies`（复用 borrow 图）+ FFI `declareDependencies`。
- `InstallExecutor` 解析 `jasmine.plugin.dependencies` + 持久化。
- `LifecycleExecutor.load` 依赖序加载（先加载未加载依赖）+ `declareDependencies` 建边；缺失依赖告警不阻断。

### P1-3 Activity 多占坑 ✅
- 新增 `PluginLaunchMode`（Standard/SingleTop/SingleTask/Transparent）+ 三个占坑 Activity 子类。
- manifest 注册四个占坑（不同 launchMode/theme）；`startPluginActivity` 加 `launchMode` 参数路由。

### P1-4 Service 跨进程 bind ✅
- 新增隔离进程 Service 池 `IsolatedHostService1-4`（`process=":plugin_isolated"`）。
- `ServiceProxyPool` 分宿主池/隔离池，按插件是否隔离路由；`bindService` 到隔离槽时 AMS 自动跨进程传递 IBinder（无需额外桥）。

### P2-6 资源释放 ✅
- `LoadedPlugin.release()`（API 30+ `assets.close()`）；`LifecycleExecutor.unload` 调用。

### P2-7 assets-cache 配额 ✅
- `PluginAssetsProvider` 加 64MB 配额，超限最旧优先淘汰。

### P2-8 ledger 跨进程一致性 ✅
- `PluginHost.refreshLedger()`（`handle.repair()`）；`IsolatedPluginProcessService` 加载前重读盘。

### P3-9 Kotlin 单测 ✅
- 加 Robolectric（offline + dependency.dir 指向本地缓存）；`PluginProcessBridgeTest` 3 个测试覆盖 register/resolve/unregister/names/local 目录。

### P3-10 可观测事件流 ✅
- 新增 `PluginEvent`（Installed/Uninstalled/Loaded/Unloaded/Isolated/IsolationReleased/Crash/Failure）+ `PluginEventListener`。
- `PluginHost.setEventListener`；安装/卸载/加载/卸载/隔离关键点发射事件。
