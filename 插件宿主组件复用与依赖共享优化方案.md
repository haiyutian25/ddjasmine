# 插件宿主组件复用与依赖共享优化方案

> 目标：让插件开发**通用化**——插件正常用 `implementation` 声明它需要的所有依赖（框架 API、第三方 SDK、与宿主重复的组件），框架**自动去重**：宿主已提供的组件直接使用宿主的、不打进插件 DEX；宿主没有的才打进 DEX。插件作者无需手写任何 `compileOnly` / `excludeGroups`，也无需因某个插件去修改框架。

---

## 一、核心结论

1. **运行时层面，框架早已"直接使用宿主组件"**：`parent-first` ClassLoader + `AssetManager` 合并宿主资源，插件里引用 Compose/Ktor/androidx 等类型运行时直接从宿主解析，无需复制。
2. **真正的痛点在构建期**：旧机制要求插件作者手动区分——`compileOnly`（从宿主用）+ `excludeGroups`（不打进 DEX），每个插件手抄一遍、版本须与宿主对齐，漏配就重复打包或编译不过。
3. **Google 官方无"运行时加载独立 APK 并直接使用宿主组件"的 API**（Play Feature Delivery 是编译期拆模块 + 商店分发，不适用）。业界共识就是 parent-first + 资源合并，本框架已落地。

---

## 二、框架现状（运行时已"直接使用宿主"）

### 1. 类加载：parent-first

`core-plugin/.../internal/PluginClassLoader.kt` 继承 `BaseDexClassLoader`，parent = 宿主 `application.classLoader`。解析顺序：宿主 PathClassLoader → 插件自身 DEX → 跨插件借类（Rust class_index）→ HostFallback。

### 2. 资源加载：AssetManager 已 pre-seed 宿主资源

`core-plugin/.../internal/PluginResourcesLoader.kt` 先把宿主 package 资源 `addAssetPath`，再叠加插件资源；package-id 分区（宿主 `0x7f`，插件 `0x80+N`）保证 id 不冲突、合并加载。

### 3. Compose UI：直接在宿主 Compose 树渲染

`PluginEntry.MainScreen()` 为 `@Composable`，运行在宿主 Compose 树内。

---

## 三、痛点定位

旧机制（`PluginPackPlugin.dependencyJars`）把 `implementation` 依赖默认**全部 d8 进插件 DEX**，靠手动 `excludeGroups` 排除、靠 `compileOnly` 声明"从宿主用"。于是 MCP 等插件被迫手抄：

```kotlin
compileOnly(libs.androidx.activity)
compileOnly(libs.ktor.client.core)
compileOnly(libs.ktor.client.okhttp)
compileOnly(composeBom)
compileOnly(ui / foundation / material3 / icons)   // ×5
excludeGroups.add("io.ktor")
excludeGroups.add("com.squareup.okhttp3")
excludeGroups.add("com.squareup.okio")
```

问题：每个插件手抄一遍；版本须与宿主对齐；漏配 `excludeGroups` 重复打包甚至类冲突，漏配 `compileOnly` 编译不过。

---

## 四、通用方案：框架自动去重

### 唯一核心：`PluginPackPlugin` 自动去重

**改动点**（`build-logic/.../PluginPackPlugin.kt`）：

1. **`dependencyJars` 只取外部模块的 jar**：用 `artifactView { componentFilter { it is ModuleComponentIdentifier } }`，在 variant 匹配前过滤掉 project 依赖（`core-plugin` 等框架模块），避免 Android library project 依赖的 variant 歧义；project 依赖的类由宿主提供、运行时 parent-first 解析、不进插件 DEX。

2. **自动排除宿主已提供的依赖**：新增 `hostProvidedGroups()` 用 `resolutionResult.allComponents` 读取宿主 `:app` 的全部传递依赖 group，凡宿主已有的 group 自动从插件 DEX 排除。

3. `hostProject` 默认 `:app`，插件模块无需任何配置。

**效果**：插件只需正常声明依赖，框架自动决定去留：

```kotlin
// sample-mcp/build.gradle.kts —— 无任何 compileOnly / excludeGroups
dependencies {
    implementation(project(":core-plugin"))        // 框架 API：project 依赖，不进 DEX，运行时用宿主
    implementation(libs.mcp.sdk.client)            // 宿主没有 → 打进 DEX
    implementation(libs.ktor.client.core)          // 宿主有 → 自动去重，用宿主的
    implementation(libs.ktor.client.okhttp)        // 宿主有 → 自动去重
    implementation(composeBom)                     // 宿主有 → 自动去重
    implementation(ui / foundation / material3 / icons)  // 宿主有 → 自动去重
    ksp(project(":core-plugin-ksp"))
}
```

任何插件（MCP、Proot、未来的任意插件）都遵循同一规则：**引入的 SDK / 组件若与宿主重复，框架自动去掉重复、直接使用宿主的**，无需针对某个插件或某个组件做任何特殊处理。

---

## 五、验证

- 构建 `:app:assembleRelease` 通过。
- `sample-mcp` 插件 DEX 仅含 `io.modelcontextprotocol.*`（MCP SDK，956 类）+ 插件自身（246 类）+ MCP SDK 传递依赖（typesafe-config / kotlin-logging），共 1457 类。
- 宿主已提供的 `io.ktor`（2509 类）、`okhttp3`、`okio`、`androidx.compose` 等全部自动去重、不再打入插件 DEX。
- 插件 APK 体积 3.3MB → 0.63MB。
