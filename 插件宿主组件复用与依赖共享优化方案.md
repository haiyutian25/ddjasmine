# 插件宿主组件复用与依赖共享优化方案

> 目标：解决 MCP 等插件开发中「复用宿主 app 组件」的痛点——让插件**直接使用**宿主组件，而不是手动配置一堆 `compileOnly` + `excludeGroups`（"复用"）。

---

## 一、核心结论

1. **运行时层面，框架早已实现"直接使用宿主组件"，无需"复用（复制代码/重复打包）"**。
2. **真正的痛点在构建期**：每个插件都要手抄一套 `compileOnly` + `excludeGroups`，且版本必须与宿主对齐。
3. **Google 官方没有**"运行时加载未安装 APK 并直接使用宿主组件"的 API。最接近的 **Play Feature Delivery**（动态功能模块）本质是"编译期拆模块 + 商店分发"，依赖共享只发生在 Gradle 编译期，**不适用于我们这种运行时动态加载独立/第三方 APK 的场景**。业界（腾讯 Shadow 等，亿级用户验证）的共识做法正是 **parent-first ClassLoader + AssetManager 合并资源**——本框架已经落地。

---

## 二、框架现状（运行时已"直接使用宿主"）

### 1. 类加载：parent-first

`core-plugin/.../internal/PluginClassLoader.kt` 继承 `BaseDexClassLoader`，parent = 宿主 `application.classLoader`。

解析顺序：

```
宿主 PathClassLoader（Compose/Ktor/OkHttp/androidx 在此命中）
  → 插件自身 DEX（MCP SDK 等）
  → 跨插件借类（Rust class_index 定位）
  → HostFallback 兜底
```

插件里写 `androidx.compose.material3.Button`、`io.ktor.client.*`，运行时**直接从宿主拿**。

### 2. 资源加载：AssetManager 已 pre-seed 宿主资源

`core-plugin/.../internal/PluginResourcesLoader.kt` 的 `newHostBasedAssets()` 先把宿主 package 资源 `addAssetPath` 进去，再叠加插件资源；构建期 package-id 分区（宿主 `0x7f`，插件 `0x80+N`）保证 id 永不冲突，两张资源表合并加载。

### 3. Compose UI：直接在宿主 Compose 树渲染

`PluginEntry.MainScreen()` 为 `@Composable`，注释原文 *"Runs entirely inside the host's Compose tree"*。

### 4. 依赖管理（痛点所在）

`build-logic/.../PluginPackPlugin.kt`：

- `implementation` 依赖（含传递）默认**全部 d8 进插件 DEX**；
- `excludeGroups` 手动排除的 group 才不打进 DEX、改从宿主 parent-first 解析；
- `compileOnly` 靠"不进 runtime classpath"天然排除。

---

## 三、痛点定位

`sample-mcp/build.gradle.kts` 为"复用宿主组件"手动写了：

```kotlin
compileOnly(libs.androidx.activity)
compileOnly(libs.ktor.client.core)
compileOnly(libs.ktor.client.okhttp)
compileOnly(composeBom)
compileOnly(ui / foundation / material3 / icons)   // ×5
excludeGroups.add("io.ktor")
excludeGroups.add("com.squareup.okhttp3")
excludeGroups.add("com.squareup.okio")
```

问题：每个插件手抄一遍；版本须与宿主对齐；漏配 `excludeGroups` 会重复打包甚至类冲突，漏配 `compileOnly` 编译不过。

---

## 四、方案设计

### 方案 A：`PluginPackPlugin` 自动宿主依赖排除（核心）

**目标**：构建插件时自动读取宿主 app 的 `releaseRuntimeClasspath`，凡宿主已有的依赖 group 自动排除出插件 DEX；插件只需写 `implementation`，不再手写 `compileOnly` + `excludeGroups`。

**改动**：`PluginPackExtension` 增加 `hostProject: Property<String>`（默认 `:app`）；`dependencyJars` 过滤逻辑在手动 `excludeGroups` 之外，追加"宿主已提供 group"的自动排除。

**效果**：`sample-mcp` 精简为：

```kotlin
pluginPack { packageIdSlot.set(4) }   // 不再需要 excludeGroups

dependencies {
    compileOnly(project(":host-api"))
    implementation(libs.mcp.sdk.client)      // 宿主没有 → 打进 DEX
    implementation(libs.ktor.client.core)    // 宿主有 → 自动排除
    implementation(libs.ktor.client.okhttp)  // 宿主有 → 自动排除
    // Compose / androidx 由默认 excludeGroups("androidx") 前缀自动排除
}
```

### 方案 B：新建 `:host-api` 模块（插件直接 import 宿主组件）

**目标**：把宿主要暴露的 API（`core-plugin` + `core-ui`）聚合成一个 `host-api` 模块，插件 `compileOnly(project(":host-api"))` 即可在编译期直接 `import` 宿主的主题、组件、`PluginHost` 等。

**改动**：新建 `:host-api`，`api(project(":core-plugin"))` + `api(project(":core-ui"))`，并附一个 `HostApi` 门面；`settings.gradle.kts` 增加 include。

### 方案 C：宿主资源直用（颜色/主题常量 + 资源）

**目标**：插件直接引用宿主主题/颜色常量（`JasmineTheme`、`JasmineWhite80` 等，经 `core-ui` 的 `api` 传递）；真正的 drawable/string 如需共享，抽到 library 模块让 R 在 library 生成、插件 `compileOnly` 引用。

**落地**：方案 B 的 `host-api` 聚合 `core-ui` 后，插件即可 `import com.lhzkml.jasmine.core.ui.JasmineTheme`、`import com.lhzkml.jasmine.core.ui.Color.kt` 的顶层颜色常量，实现"资源直用"。

---

## 五、验证

- 构建 `:app:assembleRelease`，确认 `sample-mcp` 的插件 DEX 中仅含 `io.modelcontextprotocol.*`（MCP SDK），不再含 `io.ktor` / `okhttp3` / `androidx.compose`。
- 精简后的 `sample-mcp/build.gradle.kts` 不再出现手写 `excludeGroups` 与成串 `compileOnly`。
