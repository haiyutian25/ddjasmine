# 插件化框架能力盘点与缺口分析

> 本文档对 Jasmine Android 插件化框架的现有能力做完整盘点，并列出相对业界插件化框架（Shadow / VirtualAPK / RePlugin）与插件实际开发需求的缺口，作为后续框架演进的路标。
>
> 盘点范围：`core-plugin`（框架核心）、`build-logic`（PluginPack / PluginDev）、`core-ui`、宿主 `app`、`rust` workspace、示例插件（`sample-*`）。

---

## 一、架构总览

框架是 **「Rust 决策核心 + Kotlin 执行器」的代理（占坑）模式**，不是 Shadow 式的宿主 Activity 子类 Hook，也不是 RePlugin 的动态坑位模式。

- 核心入口：`PluginHost`（`core-plugin/.../plugin/PluginHost.kt`）——install / load / unload / update / audit / gate 全经此。
- 决策核心：`PluginCoreHandle`（Rust + UniFFI 绑定，`core-plugin/.../plugin/rust/`）——签名裁决、类定位、组件路由、崩溃分类、授权。
- 代理组件：`proxy/` 下的 `HostActivity`、`HostService` + `ServiceProxyPool`、`HostReceiver`、`HostProvider`。
- 插件 UI 双模式：纯 Compose `MainScreen()` 直接宿主渲染 + 可选 `PluginActivity` 代理。

---

## 二、能力盘点（已有什么）

### 1. 类加载

- `PluginClassLoader extends BaseDexClassLoader`，parent = 宿主 `application.classLoader`，**parent-first**。
- 解析顺序：宿主 PathClassLoader → 插件自身 DEX → 跨插件借类（Rust class_index 定位）→ HostFallback 兜底。

### 2. 资源

- `PluginResourcesLoader` 用 `AssetManager` 先 `addAssetPath` 宿主 package 资源，再叠加插件资源；构建期 package-id 分区（宿主 `0x7f`，插件 `0x80+N`）保证 id 不冲突、合并加载。

### 3. 依赖去重（构建期，通用）

- `PluginPackPlugin.dependencyJars` 用 `artifactView { componentFilter { it is ModuleComponentIdentifier } }` 跳过 project 依赖，避免 variant 歧义。
- `hostProvidedGroups()` 用 `resolutionResult.allComponents` 读取宿主全部传递依赖 group，凡宿主已提供的第三方库（Ktor/OkHttp/Compose 等）自动从插件 DEX 去重。
- 插件只需正常 `implementation` 声明依赖，无需手写 `compileOnly` / `excludeGroups`。

### 4. Native / Rust

- `.so` 链路完整：AAR `jni/<abi>/` → APK `lib/<abi>/`（PluginPackPlugin）→ 提取到 `filesDir/plugins/<id>/lib/<abi>/`（InstallExecutor）→ `librarySearchPath`（LifecycleExecutor）→ `System.loadLibrary`。
- Rust workspace（`rust/`）：`ffi`（UniFFI 入口 → `libffi.so`）、`plugin-core`、`store`、`session-log`、`measure`、`compose`、`codec`、`uniffi-bindgen`。
- UniFFI 绑定生成：`core-plugin` / `core-data` 的 `generateBindings`（`cargo run -p uniffi-bindgen`）。
- executable 运行：`ExecBridge` + `libexecbridge.so`（dlopen bridge，noexec-safe），`assets/exec/` 提取 + `FfiCapability.EXEC` 门控。
- 插件 native 生命周期钩子：`PluginEntry.onNativeReady(PluginContext)` / `onNativeRelease()`——在 `.so` 可提取后、`MainScreen` 首次组合前调用，用于初始化/释放重型 native 状态。

### 5. 四大组件（宿主代理 / 占坑模式）

| 组件 | 插件侧类型 | 宿主代理 | 状态 |
|---|---|---|---|
| Activity | `PluginActivity` / `BasePluginActivity` | `HostActivity` + 4 个 launchMode 变体 | 支持（POJO 代理） |
| Service | `PluginService` / `BasePluginService` | `HostService` + 10 宿主槽 + 4 隔离槽 | 支持（POJO 代理） |
| BroadcastReceiver | `PluginReceiver` | `HostReceiver` + `StaticReceiverDispatcher` | 支持静态 + 动态 |
| ContentProvider | 插件真实 `ContentProvider` | `HostProvider` 中心化代理 | 半支持（有缺陷，见缺口） |

- 插件内跳系统界面/分享/`Settings`：可用宿主 Context 直接 `startActivity`。
- 插件自己的 Activity：走 `Context.startPluginActivity(...)`（`proxy/PluginApis.kt`）。
- 前台服务 + 通知：支持（宿主 manifest 已声明 `FOREGROUND_SERVICE*`、`POST_NOTIFICATIONS`，代理 Service `foregroundServiceType="specialUse"`）。

### 6. 安装 / 更新 / 安全

- 安装：`installPlugin` / `installBundledPlugins`（assets 通道）/ 卸载 `uninstallPlugin` / 更新 `applyAvailableUpdates`。
- 签名策略：`Strict` / `UserGrant` / `Insecure`，`Signatures` 校验宿主 + 包签名。
- 授权：`AuthorizationHandler`（Ask verdict）+ `DefaultPluginAuthorizationHandler` + `PluginAuthorizationActivity`。
- 能力门控：`EXEC` / `GPU` / `NETWORK` / `STORAGE` / `CAMERA`（`checkCapability` / `requireCapability` / `hasCapability`）。
- 敏感 API 门控：`checkApi`（`ApiRule.Host` / `SelfOrHost` / `AnyPlugin`）。
- 权限：解析 `uses-permission` + `warnMissingHostPermissions`（宿主未声明的权限告警）。
- 审计/修复：`audit()` / `repair()`（registry ↔ class index ↔ loaded 三方对账）。

### 7. 进程 / 跨进程通信

- 进程隔离：`ProcessIsolationManager`（`:plugin_isolated` 进程，`isolated_plugins.json` 持久化）。
- 跨进程能力分发：`OffloadDispatcher`（命名能力，abstract unix socket 转发宿主）。
- Binder 远程服务：`RemoteServices`（`publish` / `resolve`，进程内 bridge 目录 + 跨进程 wrap）。
- 进程桥：`PluginProcessBridge`（手写 Binder，name→IBinder 目录）。

### 8. 插件间协作

- 服务发布/消费：`ServiceTable` / `ServiceKey`，`resolveService`。
- 依赖链：`dependentsChain` / `dependenciesChain` / `restartPlan`（链式重启）。
- 菜单：`PluginMenuEntry` + `loadedMenuEntries`（动态设置列表）。
- 事件：`PluginEvent`（Installed/Uninstalled/Loaded/Unloaded/Isolated/Crash...）。

### 9. 崩溃处理

- `CrashHook`：`Thread.setDefaultUncaughtExceptionHandler` → Rust `classifyCrash` 分类归因 → 回调 `PluginCrashCallback` → 透传上一个 handler。

---

## 三、缺口清单（还缺什么）

### 高影响

| # | 缺口 | 表现 | 影响 |
|---|---|---|---|
| 1 | 进程内崩溃无法隔离续跑 | `CrashHook` 只分类后透传默认 handler，未捕获异常仍终止宿主；且 `pluginCrashCallback()` 默认 null，等于默认不装 CrashHook | 高 |
| 2 | 隔离进程插件无 UI 回传 | isolated 插件宿主不加载，`entryOf` 为 null，`MainScreen`/Activity 均不可用 | 高 |
| 3 | 插件 Activity/Service 是 POJO 非真实组件 | 不能继承 `Activity`/`AppCompatActivity`，`onActivityResult`/`onRequestPermissionsResult`/`activityResultRegistry` 未转发 | 高 |
| 4 | ContentProvider 缺陷 | ① `HostProvider.resolve` 直接 `newInstance` 不调 `attachInfo`/`onCreate`，插件 Provider `getContext()` 为 null；② `PluginApis.pluginProxyUri` 的 `uri.pathSegments.drop(1)` 误丢真实 path 首段（`/books` → `/`），多段 path 路由错误（`HostProvider.resolve` 的 `drop(1)` 是正确的，丢的是 urlEncoded 插件 authority） | 高 |
| 5 | 无 WorkManager / AlarmManager / JobScheduler | 插件无法可靠注册后台任务，无 Worker 代理或 ClassLoader 桥 | 高 |
| 6 | 无插件级数据持久化封装与隔离 | 只有 `pluginDir` 字符串；无 SharedPreferences/Room/SQLite 助手；误用宿主 Context 污染宿主命名空间 | 高 |

### 中影响

| # | 缺口 | 表现 |
|---|---|---|
| 7 | 插件权限不可运行时扩展 | `uses-permission` 只解析 + 告警，宿主 manifest 未声明的权限插件永远拿不到 |
| 8 | 无插件 Application / AppComponentFactory 生命周期 | 只能 `PluginEntry.onLoad`，无法自定义应用级初始化 |
| 9 | 资源只认打包期分区，无运行时 remap | 标准 `0x7f` 插件会冲突 |
| 10 | 进程隔离粗粒度 | 只有一个 `:plugin_isolated`，所有隔离插件共享 |
| 11 | Service 池固定容量 | 10 宿主槽 + 4 隔离槽，耗尽直接失败，不排队 |
| 12 | 插件依赖不自动安装/下载 | 依赖缺失只报告，更新通道按单插件更新 |
| 13 | 运行时授权态持久化弱 | 安装期 capabilities 落盘，但运行时 `recordGrant` 是会话级缓存 |

### 低影响

| # | 缺口 | 表现 |
|---|---|---|
| 14 | 崩溃可观测事件断裂 | `PluginEvent.Crash` 定义但从不 emit |
| 15 | Activity 代理变体有限 | 仅 Standard/SingleTop/SingleTask/Transparent |
| 16 | Native 库仅解压最佳 ABI | 其余依赖 in-APK fallback |

---

## 四、与业界对比的关键差距

1. **组件真实度低**：Activity/Service 是接口 + POJO，插件无法直接复用依赖 `Activity`/`Service` 类型的代码，迁移真实 App 成本高。
2. **无 Manifest 组件预注册/Intent 路由**：只有显式 `startPluginActivity`，无隐式 Intent 与 deep link。
3. **无多进程/多实例插件进程模型**：只有宿主 + 一个共享 `:plugin_isolated`。
4. **无运行时资源重映射**：完全依赖打包期 package-id 分区。
5. **无插件级 Context 封装**：只给宿主 `Application` + `pluginDir` 字符串。
6. **后台任务生态缺失**：无 WorkManager/AlarmManager/JobScheduler 桥接。
7. **ContentProvider 支持不完整**：缺 attachInfo/生命周期，URI 改写有 bug。

---

## 五、建议优先级

结合后续 Proot 插件（重型 native、进程隔离、可能无 UI）的需求：

1. **崩溃隔离**（#1）—— 进程内插件崩溃不能拖垮宿主
2. **隔离进程 UI 回传**（#2）—— 否则 Proot 隔离后无界面
3. **数据持久化封装**（#6）—— 插件存配置/状态的基础设施
4. **ContentProvider 修复**（#4）—— 明确 bug
5. **WorkManager 桥**（#5）—— 后台定时任务

---

## 六、增强优化方案（逐项）

> 每项含：现状 → 增强方案 → 工作量。建议按「低工作量 + 高收益」优先实施。

### 高影响

#### #6 数据持久化封装（低工作量，已实施）

- **现状**：`PluginContext` 只有 `pluginDir: String`，插件只能手搓文件；误用宿主 `Application` 会污染宿主命名空间。
- **最终方案（已实施）**：持久化统一走宿主已有的 Room（`core-database`），框架不重复实现 SharedPreferences：
  1. `PluginContext.databaseDir()`——插件级 Room 数据库目录（`pluginDir/databases/`），插件复用宿主 Room 依赖（方案 A 自动去重）定义自己的 Entity/DAO，数据库落在该目录隔离；
  2. `filesDir()` / `openFileInput` / `openFileOutput`——文件读写助手（基于 `pluginDir`）；
  3. 卸载 `uninstallPlugin` 删除 `pluginDir` 时数据库/文件一并清理。
- **工作量**：低（约 50~80 行）。

#### #4 ContentProvider 修复（bug 修复，直接）

- **现状**：① `HostProvider.resolve` 直接 `newInstance` 不调 `attachInfo`/`onCreate`，插件 Provider `getContext()` 为 null；② `PluginApis.pluginProxyUri` 的 `pathSegments.drop(1)` 误丢真实 path 首段。
- **方案**：
  1. `HostProvider.resolve` 实例化后补 `provider.attachInfo(context, providerInfo)` + `provider.onCreate()`；
  2. `pluginProxyUri` 去掉 `drop(1)`，改为 `uri.pathSegments.forEach { appendPath(it) }`。
- **工作量**：低（约 10 行）。

#### #1 崩溃隔离（低-中工作量，已实施「崩溃熔断」）

- **现状**：`CrashHook` 只分类后透传默认 handler（仍终止进程），且 `pluginCrashCallback()` 默认 null 导致默认不装。
- **方案**（按性价比）：
  1. ~~Compose 错误边界~~：**不可行**——Compose 编译器禁止 try-catch 包裹 composable 调用（`Try catch is not supported around composable function invocations`）；
  2. **崩溃熔断（已实施）**：`CrashHook.install` 归因到插件后，同步写 `<pluginId>.crash` 标记；`PluginHostApplication` 启动时读取标记并在 `loadFilter` 跳过，避免反复崩溃拖垮宿主；
  3. **崩溃事件 emit（已实施，见 #14）**：归因后 `emit(PluginEvent.Crash)`；
  4. **生命周期回调 try-catch**（可选）：`HostActivity`/`HostService` 的插件回调统一 `runCatching`，单个回调异常不致命。
- **彻底隔离**依赖进程隔离（见 #2）。
- **工作量**：低-中。

#### #3 POJO 组件增强（低工作量，已实施）

- **现状**：`PluginActivity` 接口缺 `onActivityResult`/`onRequestPermissionsResult`/`onNewIntent`，插件无法处理结果回传。
- **方案（已实施）**：`PluginActivity` 接口 + `HostActivity` 补转发 `onActivityResult`/`onRequestPermissionsResult`/`onNewIntent`；`BasePluginActivity` 暴露 `activityResultRegistry`（官方推荐替代已废弃的 `onActivityResult`）。`PluginService` 同理可补 `onTaskRemoved`。
- **工作量**：低。

#### #5 WorkManager 桥（中-高工作量，轻量档已实施）

- **现状**：core-plugin 无 WorkManager/AlarmManager/JobScheduler 集成，插件无法可靠注册后台任务。
- **方案**（两档）：
  1. **轻量档（已实施）**：`PluginScheduler`（`process/PluginScheduler.kt`）提供 `scheduleOnce`/`schedulePeriodic`/`cancel`，基于 `AlarmManager` + `PluginAlarmReceiver` + Service 池 + 前台服务；已声明 `SCHEDULE_EXACT_ALARM` 权限并在无权限时降级为非精确触发（Android 12+ 要求）；
  2. **完整档（未做）**：宿主声明一个 `PluginWorkerProxy`（`CoroutineWorker`），插件经 ClassLoader 桥注册 Worker 实现。
- **工作量**：中-高。

#### #2 隔离进程 UI 回传（已实施：UI 伴侣双入口机制）

- **现状**：isolated 插件宿主不加载，`entryOf` 为 null，UI 无法显示；进程隔离与 UI 能力互斥。
- **已实施方案**：插件 manifest 可选声明 `jasmine.plugin.uiEntryClass`（宿主侧 UI 入口）。框架对 isolated 插件在宿主进程加载 UI 入口（轻量 UI，跳过 `onNativeReady` 和 receivers/providers 注册），隔离进程加载主入口（native 逻辑），两者经 `RemoteServices` 通信。
- **改动**：`InstallExecutor`（`Metadata.uiEntryClass` + `ui_entry` 持久化）、`LifecycleExecutor`（`uiOnly` 入口决策 + 跳过 native/组件）、`PluginHost`、`PluginHostApplication`（loadFilter 不再排除 isolated）。
- **工作量**：中。

### 中影响

| # | 缺口 | 增强方案 | 工作量 |
|---|---|---|---|
| 13 | 授权持久化弱 | **已实施**：授权账本复用宿主 Room（`core-database` 的 `PluginGrant` 表）+ 启动回放到 Rust + 卸载清理（不再用 JSON 文件） | 低 |
| 11 | Service 池固定容量 | **已实施**：槽位 10→20 宿主、4→8 隔离（`HostServicePool` + manifest） | 低 |
| 14 | 崩溃事件断裂 | **已实施**：`CrashHook` 归因后 `emit(PluginEvent.Crash)` + `observePreviousNativeCrash`（ApplicationExitInfo 观测 native 崩溃，Java handler 无法捕获的场景） | 低（几行） |
| 7 | 权限不可运行时扩展 | **已实施**：插件在自己的 manifest 声明权限，构建期 `generatePluginPermissions` 自动扫描合并进宿主（无需框架预声明权限池）+ `PluginHost.hasPermission`/`requestPermission` 运行时请求 | 中 |
| 8 | 无插件 Application 生命周期 | **已实施**：插件 `<application android:name>` 经反射 `attach` + `onCreate`，`onLowMemory`/`onTrimMemory` 由宿主转发（用 `appInfo.className` + 文件持久化，无需改 Rust） | 中 |
| 10 | 进程粒度粗 | **已实施**：多进程槽 `:plugin_isolated_2..4`，按负载分配，`RemoteServices` 遍历所有 bridge | 中 |
| 12 | 依赖不自动安装 | **已实施**：`PluginHost.installWithDependencies` + `PluginUpdateChannel.installLatest` | 中 |
| 9 | 资源无运行时 remap | **已实施校验**：`packageIdSlot` 合法性校验（1..127）；运行时 remap 业界不做（Shadow/Shiply/Phantom 均打包期分区，本框架已同方案） | 高 |

### 低影响

| # | 缺口 | 增强方案 | 工作量 |
|---|---|---|---|
| 15 | Activity 代理变体有限 | **已实施**：增加 singleInstance 代理变体 | 低 |
| 16 | Native 仅解压最佳 ABI | **已实施**：`.so` 改为 STORED（未压缩）打包，确保 in-APK fallback 可靠（之前 DEFLATED 压缩会导致 fallback 失败） | 低 |

---

## 七、实施建议（第一批）

按「低工作量 + 高收益」先做这 5 项，一次提交：

1. **#4 ContentProvider 修复**（~10 行，纯 bug）
2. **#6 数据持久化封装**（~80 行，基础设施）
3. **#14 崩溃事件 emit**（几行）
4. **#1 崩溃隔离第一层**（Compose 错误边界 + 崩溃熔断，~60 行）
5. **#3 POJO 组件增强**（onActivityResult 等转发，~40 行）

第二批（中工作量）：#13 授权持久化、#11 Service 池扩容、#5 WorkManager 轻量档。

第三批（需权衡/复杂）：#2 隔离 UI 回传、#8 插件 Application、#10 进程粒度、#7 权限池。
