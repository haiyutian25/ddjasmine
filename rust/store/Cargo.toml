[package]
name = "store"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Append-only persistence backends (JSONL; SQLite phase 2) for the Android data spine"

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
