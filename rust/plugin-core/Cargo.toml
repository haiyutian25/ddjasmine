[package]
name = "plugin-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Dynamic plugin framework core: registry ledger, dependency topology, permission charter"

[dependencies]
store = { path = "../store" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
