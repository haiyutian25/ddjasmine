[package]
name = "session-log"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Session log engine: append-only, seq-continuous, versioned header, model-history projection"

[dependencies]
store = { path = "../store" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3"
