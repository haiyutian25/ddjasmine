# 插件化框架第四轮审查结果（安全与缺陷）

> 记录时间：2026-08-25
> 审查方式：三个独立 code-explorer 代理分片复查 + 主审逐行复核高危项。
> 与前三轮（已修 34 项，见 `插件化框架bug审查结果.md`）不重复。
> 状态标记：⬜ 待修 / 🔧 修复中 / ✅ 已修
> **本轮 16 项已全部修复**（2026-08-25），`:app:assembleRelease` 通过。

本轮核心是 **4 个高危安全缺陷**，均源于"插件与宿主同进程 + 缺少调用方身份识别 / 对端鉴权"这一威胁模型缺口。

---

## 一、高危（安全 / 确定性状态错误）

### ✅ H1. ExecBridge 能力门控可冒充 + 路径穿越 → 跨插件执行任意沙箱文件
`proxy/ExecBridge.kt:27-30,43,63`
- `requireCapability(EXEC, pluginId)` 只校验调用方自己传入的 pluginId，全库无调用方身份识别；插件与宿主同进程，可用 `allPlugins()` 枚举出任何声明 EXEC 的插件 id 传入，执行受害插件二进制。
- `executablePath` 的 `name` 未校验，`File(execDir, "../../<victim>/exec/tool")` 合法；`runNative` 走 dlopen 桥不受 noexec 限制，可加载沙箱内任意可读文件。
- **修复方向**：路径规范化并限定在本插件 execDir 内；引入调用方身份（ThreadLocal 由框架在插件调用边界设置），ExecBridge 校验调用方==声明者。

### ✅ H2. 授权结果广播可被同进程插件伪造 → 用户授权被批量"批准"
`auth/PluginAuthorizationActivity.kt:60-66,84,99-104`
- 结果回传是无权限保护的裸广播（仅 setPackage）；`RECEIVER_NOT_EXPORTED` 挡跨应用但挡不住同 UID（插件跑在宿主进程）。
- `requestCode` 从 `AtomicInteger(1)` 顺序递增、可预测；恶意插件广播 `granted=true` + code=1..N 即可批准任何挂起授权。
- **修复方向**：requestCode 改为 `SecureRandom` 不可预测 token，盲猜伪造失效。

### ✅ H3. 自定义 action 动态接收器 `RECEIVER_EXPORTED` + `intent.package` 判内部 → 外部可触达 exported=false 插件接收器
`proxy/StaticReceiverDispatcher.kt:72` + `proxy/HostReceiver.kt:43`
- 插件自定义 action 动态注册为 `RECEIVER_EXPORTED`（外部可达）；`isInternal = intent.package == packageName` 是攻击者可控字段。
- 第三方 `sendBroadcast(Intent("custom.ACTION").setPackage(宿主包名))` → `isInternal=true` → 绕过 Rust 侧 exported=false 门控。
- **修复方向**：动态接收器改 `RECEIVER_NOT_EXPORTED`（与 manifest HostReceiver 一致的安全默认），收到的广播即可信为内部。

### ✅ H4. 卸载隔离插件不回收隔离进程/服务，`unmarkIsolated` 死代码 → 卸载后"双活"
`PluginHost.kt:515-526`
- `uninstallPlugin` 只 `lc.unload`（宿主 UI 伴侣）+ 删文件，不调 `releaseIsolation`/`ProcessIsolationManager.release`。
- `unmarkIsolated` 零调用点；隔离进程主入口继续运行（ClassLoader 指向已删 payload）、服务存活、`isolated` 映射与 `isolated_plugins.json` 不清理、槽位仍占用。
- **修复方向**：`uninstallPlugin` 内调用 `ProcessIsolationManager.release(pluginId)` 回收隔离。

### ✅ H5. Offload abstract socket 无对端鉴权 + 固定名称可被抢绑劫持
`process/OffloadDispatcher.kt:38` + `process/AbstractSocketChannel.kt:58-72`
- `SOCKET_NAME="jasmine-offload"` 全局常量、不含包名：同设备第二个宿主抢绑失败被静默吞；SELinux 放行时其请求被第一个宿主 handler 执行。
- 服务端 accept→handle 全程无 `getPeerCredentials()` 对端鉴权，任何本机进程可用任意 argv 调用全部命名能力，绕过 `requireCapability`。
- **修复方向**：socket 名拼入 `packageName`；服务端用 `LocalSocket.getPeerCredentials()` 校验对端 UID==本应用 UID。

## 二、中危

| # | 位置 | 问题 | 修复方向 |
|---|---|---|---|
| ✅ M1 | `PluginProcessBridge.kt:210-213` | `acquireServiceSlotLocal` check-then-poll 非原子 → 代理类永久泄漏 + 双实例 | 对 check+poll+登记整体加锁 |
| ✅ M2 | `HostService.kt:104-109` | 宿主侧 `release` 对 bridge `forEach` 无异常防护 → DeadObjectException 中断 + 槽位泄漏 + 顶替原始异常 | 每个 bridge 调用包 runCatching |
| ✅ M3 | `HostService.kt:188-194` | 隔离进程未初始化完成宿主即可启动其池服务 → 服务静默死亡但返回 true | 隔离服务 init 前 await PluginHost ready |
| ✅ M4 | `AbstractSocketChannel.kt:74-114` | socket 无超时、服务端每连接无限开线程 → 恶意插件拖垮宿主 | setSoTimeout + 并发上限 |
| ✅ M5 | `PluginResourcesLoader.kt:98-134` | `loadAssetFd` 并发提取无互斥 → 缓存撕裂/配额删在途文件 | 临时文件 + 原子 rename / 按 path 加锁 |

## 三、低危 / 潜在

| # | 位置 | 问题 |
|---|---|---|
| ✅ 低1 | `InstallExecutor.kt:185-203` | ABI 选择用设备 SUPPORTED_ABIS 而非进程位宽，32 位宿主进程提取 64 位 .so |
| ✅ 低2 | `PluginProcessBridge.kt:235-242` | TX_REGISTER 对 null binder → NPE；null name 落空键 |
| ✅ 低3 | `PluginResourcesLoader.kt:36-41` | loadFromApk 抛异常时 PFD 泄漏（加载失败路径） |
| ✅ 低4 | `PluginResourcesLoader.kt:63-68` | API<30 addAssetPath 返回 0 不校验，资源静默缺失 |
| ✅ 低5 | `AbstractSocketChannel.kt:118-131` | writeFrame 无上限，与 readFrame 1MiB 不对称 |
| ✅ 低6 | `PluginResourcesLoader.kt:99-114` | loadAssetFd runCatching 吞全部异常（含 OOM/磁盘满） |

## 复核说明
- 主审逐行确认：H1/H2/H3/H4、M1/M2（均真实）。
- 代理报告带行号与代码片段：H5、M3/M4/M5、低 1-6。
- 三代理确认未重复上报前三轮已修的 34 项；帧协议、目录并发、Manifest 声明、zip 路径穿越经核查无问题。

---

# 修复记录（2026-08-25，全部 16 项已实施，`:app:assembleRelease` 通过）

## 高危（安全）

| 项 | 修复方式 | 涉及文件 |
|---|---|---|
| H1 | `executablePath` 规范化后强制限定在本插件 `execDir` 内，杜绝 `../` 逃逸到其它插件 native 库/base.apk/任意沙箱文件（dlopen 不受 noexec 限制）。**残留说明**：`requireCapability(EXEC, pluginId)` 仍信任调用方自报的 pluginId——同进程插件无调用方身份识别，彻底关闭需进程级隔离（超出本轮范围，已在代码注释与本文档标注） | `proxy/ExecBridge.kt` |
| H2 | 授权 `requestCode` 由顺序 `AtomicInteger` 改为 `SecureRandom` 32 位 token，盲猜伪造（code=1..N 批量批准）不可行；叠加 `RECEIVER_NOT_EXPORTED` 挡跨应用 | `auth/PluginAuthorizationActivity.kt` |
| H3 | 自定义 action 动态接收器 `RECEIVER_EXPORTED`→`RECEIVER_NOT_EXPORTED`，第三方 `setPackage(宿主)` 伪造"内部广播"触达 exported=false 插件接收器的路径被切断，与 manifest HostReceiver 安全默认一致 | `proxy/StaticReceiverDispatcher.kt` |
| H4 | `uninstallPlugin` 增加 `ProcessIsolationManager.release(pluginId)`，卸载时回收隔离进程/服务、清理槽位与 isolated 映射，消除"双活" | `PluginHost.kt` |
| H5 | socket 名拼入包名（`jasmine-offload.<pkg>`，`OffloadDispatcher.configure` 每进程设置）避免多宿主抢绑/劫持；服务端 `accept` 后用 `LocalSocket.getPeerCredentials().uid` 校验对端==本应用 UID，拒绝其它应用进程连入执行命名能力 | `process/OffloadDispatcher.kt`、`process/AbstractSocketChannel.kt`、`PluginHostApplication.kt` |

## 中危

| 项 | 修复方式 | 涉及文件 |
|---|---|---|
| M1 | `acquireServiceSlotLocal` 加 `@Synchronized`，check→poll→登记三步原子，消除同 instanceId 并发导致的代理类泄漏/双实例 | `process/PluginProcessBridge.kt` |
| M2 | 宿主侧 `release`/`proxyClassOf`/`acquire` 及 `RemoteServices.resolve` 对每个 bridge 的 transact 包 `runCatching`，死桥 `DeadObjectException` 不再中断循环/泄漏槽位/顶替原始异常 | `proxy/HostService.kt`、`process/RemoteServices.kt` |
| M3 | `HostService.initPluginService` 在 `PluginHost` 未就绪时 `runBlocking` 等待 `awaitReady`（5s 超时），避免隔离进程初始化未完成即启动池服务导致静默死亡 | `proxy/HostService.kt` |
| M4 | socket 双侧 `setSoTimeout(10s)`；服务端并发连接上限 32，超限拒绝，防止恶意插件开大量连接拖垮宿主 | `process/AbstractSocketChannel.kt` |
| M5 | `loadAssetFd` 按 path 加锁 + 临时文件原子 `renameTo`，消除并发提取同名缓存的截断/撕裂与配额删在途文件 | `internal/PluginResourcesLoader.kt` |

## 低危

| 项 | 修复方式 | 涉及文件 |
|---|---|---|
| 低1 | ABI 选择按 `Process.is64Bit()` 过滤，32 位宿主进程不再提取 64 位 .so | `internal/InstallExecutor.kt` |
| 低2 | `TX_REGISTER` 服务端对 null binder/空名判空拒绝，避免 `ConcurrentHashMap` NPE 与空键污染 | `process/PluginProcessBridge.kt` |
| 低3 | `loadFromApk` 抛异常时关闭已打开的 PFD（加载失败路径泄漏） | `internal/PluginResourcesLoader.kt` |
| 低4 | API<30 `addAssetPath` 返回 0（失败）时抛错，不再静默缺失资源 | `internal/PluginResourcesLoader.kt` |
| 低5 | `writeFrame` 增加 `MAX_FRAME_BYTES` 上限校验，与 `readFrame` 对称 | `process/AbstractSocketChannel.kt` |
| 低6 | `loadAssetFd` 由 `getOrNull` 静默吞异常改为 `getOrElse` 记录真实原因 | `internal/PluginResourcesLoader.kt` |

## 遗留（需架构决策，非本轮范围）

- **H1 能力门控冒充**：同进程插件无调用方身份识别，`requireCapability` 信任自报 pluginId。彻底关闭需为插件引入进程级隔离或统一调用方上下文（ThreadLocal 在插件自启线程/Compose 下不可靠）。路径穿越已关闭，残留为"运行另一已声明 EXEC 插件的二进制"，严重度显著降低。
