<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:label="PRoot 探针插件">
        <!-- Runtime contract: entry class + description read at install -->
        <meta-data
            android:name="jasmine.plugin.entryClass"
            android:value="jasmine.sample.prootprobe.ProotProbeEntry" />
        <meta-data
            android:name="jasmine.plugin.description"
            android:value="PRoot 可行性探针：在插件目录实测 execve / mmap / memfd" />
    </application>

</manifest>
